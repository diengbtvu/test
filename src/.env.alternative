# Alternative Port Configuration (Tránh trùng port)
# Sử dụng file này nếu muốn port khác

# Database Configuration
DB_PASSWORD=YourStrong@Passw0rd
DB_NAME=UltraBus
DB_USER=sa

# Alternative High Ports (9000+ range)
API_PORT=9080
ADMIN_PORT=9081
WEB_PORT=9082
DB_PORT=9434

# For environments with different port requirements
ASPNETCORE_ENVIRONMENT=Production
NODE_ENV=production
NEXT_PUBLIC_API_URL=http://localhost:9080
